import React, { useContext } from 'react';

const InputHook = ({control,...props}) => {
    const {field} = useC
    return (
        <input className="p-4 border-gray-100 bg-white outline-none focus:border-blue-400 transition-all">
        </input>
    );
};

export default InputHook;